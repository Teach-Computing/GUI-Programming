<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:CounterViewModel/>
    </Window.DataContext>
    <Grid Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel>
            <!-- Display Counter -->
            <TextBlock Text="{Binding Count}" FontSize="36" HorizontalAlignment="Center" />

            <!-- Increment Button -->
            <Button Command="{Binding IncrementCommand}" Content="Increment" FontSize="16" Width="100" HorizontalAlignment="Center" Margin="10" />
        </StackPanel>
    </Grid>
</Window>
