<Window x:Class="WpfAppEfCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfAppEfCore"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Background" Value="#4CAF50" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="DarkGreen" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" 
                   Text="Product Management System" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="10" />

        <!-- DataGrid -->
        <DataGrid x:Name="ProductGrid" Grid.Row="1" Grid.Column="0" 
                  AutoGenerateColumns="True" Margin="10"
                  VerticalAlignment="Stretch" 
                  HorizontalAlignment="Stretch" />

        <!-- Button Panel -->
        <StackPanel Grid.Row="1" Grid.Column="1"
                    VerticalAlignment="Top" 
                    HorizontalAlignment="Center" Margin="10">
            <Button Content="Add Product" Width="150" Click="AddButton_Click" />
            <Button Content="Modify Product" Width="150" Click="ModifyButton_Click" />
            <Button Content="Delete Product" Width="150" Click="DeleteButton_Click" />
        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Text="Developed with WPF  EF Core" 
                   FontSize="12" FontStyle="Italic" 
                   HorizontalAlignment="Right" Margin="10" />
    </Grid>
</Window>
